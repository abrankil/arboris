# Cierre técnico — H16 C1.3 R1.1

**Fecha:** 2026-09-20  
**Rama:** `fix/h16-c13-r11-variability-boundary`  
**Alcance:** cierre autónomo de H16 C1.3 R1.1; no incluye anotación ni evaluación de CH-003.  
**Estado:** PASS técnico; las pruebas de alcance pasan tras corregir hallazgos de esta sesión. Commit de cierre `24f6a8ce9891a6fd40d898c9144eca0b3e770482` fue publicado en la rama. Según la comunicación de Álvaro, Alejandra toma el relevo desde la planificación de CH-003 y Álvaro no necesita continuar con la ejecución de la anotación humana de CH-003 (primer ítem sin marcar de la Secuencia pendiente, sección 27 de `HITO16_CHARACTER_EXTRACTION_REFERENCE.md`). Esta nota registra el relevo; no afirma que la anotación ni la evaluación hayan comenzado.

## Unidad que se cierra

Se cierra el incremento R1.1 del contrato compartido de evidencia H16 y su frontera con ACE. La unidad establece que el registro botánico canónico conserva sus relaciones y estados, mientras que la vista computable de ACE contiene solo caracteres elegibles. Los estados canónicos no computables siguen siendo descriptivamente accesibles y explícitamente inelegibles para inferencia. La variabilidad estructural de esos caracteres debe preservarse como dato descriptivo sin alterar el resultado de inferencia de ACE.

## HEAD y commits de la unidad

HEAD al iniciar el cierre: `f811130497cfd298d5883015578fed20a5ecf817` (`H16 C1.3 R1.1: add structural variability isolation regression`).

La unidad R1.1 está representada por los cuatro commits consecutivos desde `6c0a1d2` hasta `f811130`:

- `6c0a1d2` — separar relaciones canónicas y computables.
- `2c4d76b` — preservar estado canónico de elegibilidad.
- `79e598c` — probar preservación de estado no computable.
- `f811130` — añadir regresión de aislamiento de variabilidad estructural.

## Invariantes y evidencia esperada

- **Separación canónico/computable:** `allRelations` y `allRelationsBySpecies` conservan el registro de `species_characters.json`; `relations` y `relationsBySpecies` solo exponen a ACE relaciones de caracteres computables.
- **Estados no computables:** `allCharactersById` conserva estados como `retirado` y `pendiente_revision`; la evaluación de elegibilidad devuelve `non_computable_character` y el estado canónico. La observación puede validarse estructuralmente, pero la conversión a evidencia ACE la rechaza explícitamente.
- **Aislamiento de variabilidad estructural:** una entrada de variabilidad de un carácter no computable queda disponible en `variabilityBySpecies`, no aparece en la vista computable, y no cambia el filtrado de candidatos.

Las suites indicadas abajo pasan y ejercitan los tres invariantes con el dataset y el contrato vigentes.

## Pruebas del alcance

Ejecutadas el 2026-09-20:

- `npm.cmd run test:canonical-identification` — **PASS**, 26/26.
- `npm.cmd run test:character-observers` — **PASS**, 45/45.

Total del alcance: **71/71 pruebas PASS**, cero fallas, cero omitidas.

El primer intento de ambas suites falló: se detectó que el dataset retornado no exponía `computableStatus`, lo que hacía que todos los caracteres activos fueran evaluados como no computables. Además, los tests R1.1 asumían que `CH-007` tenía una relación canónica, pero los datos actuales solo incluyen la relación no computable de `CH-024`; también había una expectativa antigua en el texto de error. Se corrigió la exposición del estado configurado y se actualizaron los tests para usar `CH-024` y el mensaje vigente. Ambas suites se volvieron a ejecutar completas y pasaron.

## Auditoría breve

### AUDITORÍA

La unidad tiene límites verificables en dataset canónico, contrato de observación y transferencia de evidencia a ACE. El alcance no modifica datos fuente botánicos ni inicia el experimento de CH-003. La elegibilidad está definida por el estado canónico y no por inferencia del motor. Las 71 pruebas pertinentes pasan.

### INCONSISTENCIAS

Se encontraron y corrigieron tres inconsistencias de pruebas/contrato durante la sesión de cierre: falta de `computableStatus` en el dataset retornado, fixture R1.1 referido a `CH-007` sin relación canónica vigente, y regex con mensaje de error anterior. Después de las correcciones, las suites completas pasan. No quedan inconsistencias conocidas en este alcance.

**Adenda de auditoría de seguimiento (2026-09-20, posterior a la entrega):** el commit `8593ead` ("H16 C1.3 R2.1: preserve historical character registry"), previo a esta unidad en la misma rama, agregó dos argumentos nuevos a la llamada a `validateVariabilityEntries` sin actualizar su firma de 6 parámetros, desalineando posicionalmente `contextsById`, `sourcesById` y `relationsBySpecies` — este último terminaba recibiendo el valor de `contextsById`, y el `relationsBySpecies` real quedaba como argumento sobrante ignorado, rompiendo la validación de relación canónica para entradas de variabilidad. El commit `6c0a1d2` de esta misma unidad corrige esa desalineación como efecto colateral del refactor de separación canónico/computable; no requiere corrección de código adicional. Se documenta aquí solo por trazabilidad, ya que no había sido registrada como inconsistencia encontrada en la sesión original.

### VACÍOS / OMISIONES

Hito 16 mantiene abiertos anotación humana de CH-003, cobertura por clase, revisión del split y derivados, baseline, comparación con LeafMachine2, observador mínimo, benchmark del holdout, decisiones OPEN, reauditoría y aprobación formal. Todo lo anterior está fuera de este cierre y no se inicia aquí.

### REDUNDANCIAS

`allRelations*` y `relations*` son vistas paralelas intencionales: la primera representa autoridad/registro canónico completo y la segunda la proyección computable de ACE. No se consolidan porque tienen semánticas distintas.

## Clasificación de artefactos locales sin seguimiento

Inventario final observado tras publicar el primer commit de cierre:

- `common-evidence-e1a.txt` — evidencia temporal de ejecución; explícitamente fuera del commit y debe permanecer intacto.
- `alvaro-vs-main.txt` — archivo vacío (0 bytes) en el momento de este registro; no contiene diagnóstico comparativo pese a su nombre. No normativo, no incluido.
- `main-vs-alvaro.txt` — diagnóstico comparativo local entre `main` y la rama de Álvaro; no normativo, no incluido.
- `GUIA_SESION_H16_2026-09-20.svg` — ayuda visual local de orientación; no forma parte de C1.3 R1.1, no incluida.

No se hace pull, merge ni rebase de `main`, ni se resuelve su divergencia. El punto de partida acordado para el relevo es la planificación de CH-003 a cargo de Alejandra. La anotación y evaluación permanecen fuera de este cierre y no comenzaron en esta secuencia.

## Entrega

Entrega del primer commit de cierre:

- HEAD publicado al registrar esta entrega: `24f6a8ce9891a6fd40d898c9144eca0b3e770482`.
- Commit nuevo de cierre: `24f6a8ce9891a6fd40d898c9144eca0b3e770482` — `H16 C1.3 R1.1: close and audit unit`.
- Pruebas: 71/71 PASS.
- Auditoría: PASS técnico; relevo a planificación de CH-003 asumido por Alejandra, según comunicación de Álvaro. Álvaro no continúa con la ejecución de la anotación humana de CH-003 (sección 27 de `HITO16_CHARACTER_EXTRACTION_REFERENCE.md`).
- `git status --short` observado:

```text
?? GUIA_SESION_H16_2026-09-20.svg
?? alvaro-vs-main.txt
?? common-evidence-e1a.txt
?? main-vs-alvaro.txt
```

- Limitaciones para CH-003: esta rama no contiene anotación humana ni evidencia de desempeño del observador visual; Alejandra inicia desde la planificación y deberá decidir criterios y punto de partida de ejecución.
